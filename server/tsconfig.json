{
  "extends": "../tsconfig.json",
  "outDir": "./dist/server" /* Redirect output structure to the directory. */,
  "rootDir": "./server/src" /* Specify the root directory of input files. Use to control the output directory structure with --outDir. */
}
